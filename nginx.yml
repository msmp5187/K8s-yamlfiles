apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  namespace: prod123
  labels:
    env: prod
    app: frontend 
spec:
  containers:
  - name: nginx
    image: nginx:1.14.2
    ports: 
    - containerPort: 80 
